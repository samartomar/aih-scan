# Contracts published by `@aihq/scan`

Every format this package produces or reads across a package boundary, with the source
that defines it, the field that carries its identity, the bound on its size, and the
exact refusal a reader produces for a version it does not know.

This document is not the contract. The source is. Each row is pinned by
`tests/contracts/contract-inventory.test.ts`, which re-reads the constants and the named
source lines, so a renamed constant, a bumped literal or a moved definition fails CI
rather than leaving this page quietly wrong.

Scanner evidence carries no authority. Nothing named here approves a subject, creates a
Core decision, establishes an organization trust root, qualifies a component, or proves
successful Core custody.

## Reading this table

- **Identity** is the field a reader checks before it reads anything else.
- **Refusal** is what a reader returns or throws for an identity it does not know. Every
  refusal is named: an unknown version is never read as though it were a known one.
- **Mirror** names any place the same value is restated, and how drift is detected.

## Evidence Scan produces

| Contract | Authoritative definition | Identity | Bound | Refusal of an unknown identity | Mirror |
| --- | --- | --- | --- | --- | --- |
| Scan attestation v2 envelope | `src/observation/scan-attestation-v2.ts:225` | `predicateType: "https://aih.dev/ScanAttestationV2"`, `payloadType: "application/vnd.in-toto+json"`, `_type: "https://in-toto.io/Statement/v1"` | decoded payload 2 MiB (`src/observation/scan-attestation-v2.ts:316`, and again before DSSE PAE at `src/observation/scan-attestation-v2.ts:740`); no separate envelope byte bound | a `ZodError` from `parseScanAttestationEnvelopeV2Json` whose issue path names `payloadType`, `_type` or `predicateType`; other malformed or noncanonical input throws `TypeError("invalid ScanAttestationV2: …")` | none |
| Scan attestation v2 predicate | `src/observation/scan-attestation-v2.ts:228` | `protocol: "ScanAttestationV2"` | as above | a `ZodError` whose issue path is `predicate.protocol` | none |
| Scan candidate v2 | `src/observation/scan-attestation-v2.ts:132` | `protocol: "ScanCandidateV2"` | `candidate.json` 2 MiB when read from a capture bundle (`src/observation/scan-bundle-v2.ts:116`) | a `ZodError` from `parseScanCandidateV2Json` or `createScanCandidateV2` whose issue path is `protocol` | none |
| Core contract identity inside the candidate | `src/observation/scan-attestation-v2.ts:138` | `coreContract.commit` and `coreContract.decisionSchemaSha256`, together one accepted pair | two strings | a `ZodError` from `parseScanCandidateV2Json` or `createScanCandidateV2`: its issue path is `coreContract.commit` or `coreContract.decisionSchemaSha256` for a value outside the accepted sets, and `coreContract` for an accepted commit declared with another accepted commit's digest | the same accepted pairs in `src/core/core-contract-lock-v2.ts:19` |
| Verified attestation facts | `src/observation/scan-attestation-v2.ts:265` | custody, not a version: only a value minted by `verifyScanAttestationV2` is accepted | n/a | `isVerifiedScanAttestationV2` (`src/observation/scan-attestation-v2.ts:1073`) returns `false` for a structurally identical literal, and each reader returns `{status:"unverified"}` | none |
| Source seal v2 | `src/observation/source-seal-v2.ts:40` | `protocol: "SourceSealV2"`, `algorithm: "code-unit-canonical-json-v1"` (`src/observation/source-seal-v2.ts:41`) | 4096 entries, 16 MiB per file (`src/observation/source-seal-v2.ts:24`), 256 MiB total (`src/observation/source-seal-v2.ts:25`), 512 KiB canonical seal (`src/observation/source-seal-v2.ts:26`) | a `ZodError` for a schema violation, whose issue path names `protocol` or `algorithm` for an unknown identity; the checks after the schema (ordering, total bytes, canonical seal bytes, bindings) throw `TypeError("invalid SourceSealV2: …")` | Core re-declares the same shape by hand in `ai-harness/src/org-policy/governance-input-v1.ts`; a structural mirror, not a digest lock |
| Source observation seal v1 | `src/observation/source-observation-seal-v1.ts:138` | `protocol: "SourceObservationSealV1"`; entries `file`, `directory`, `file-link` (link path, the real target relative to the root, the target's bytes) and `directory-link` (recorded, never traversed), in code-unit path order; `sourceTreeSha256`, `selectedClosureSha256`, `sealedSnapshotSha256` over canonical JSON; bounds `SOURCE_OBSERVATION_SEAL_LIMITS_V1` (`src/observation/source-observation-seal-v1.ts:41`): 100,000 entries, 256 MiB of file bytes, one file may use the whole budget | accepts every tree Core's `assertTrustTreeSafe` accepts within those bounds: an empty root, an empty selection, links whose real path stays inside the root (file links by link path and target bytes, directory links recorded but not traversed, as Core's trust inventory does). The analyzer snapshot of an observation run accepts exactly the same links (`links: "observation"`, `src/baseline/batch-v1.ts`): a file link, absolute or chained, is copied as its target's bytes at the link path, and a directory link is not copied, so the snapshot holds no link and an analyzer sees what the seal records; a link that appears in it during the run fails the run. A tree over a bound, a hard-linked file, a broken link, a link leaving the root, anything that is not a file, directory or such link, and a selected path that is not a file or file link are refused `subject-requirement-unmet` before anything runs, never partly sealed. The analyzer snapshot copies links as links (Windows relative targets are checked in portable form) and takes files up to the same 256 MiB budget. Known gap: Core does not visit its skip directories, so it accepts a hard link or an escaping link inside one; Scan seals the whole tree and refuses it |
| Scan capture bundle v2 | `src/observation/scan-bundle-v2.ts:130` | `manifest.protocol: "ScanBundleV2"` | 128 annexes, 16 MiB each (`src/observation/scan-bundle-v2.ts:171`) | `TypeError("invalid ScanBundleV2: …")` | none |
| Scanner manifest v1 | `src/observation/scanner-manifest-v1.ts:50` | `protocol: "ScannerManifestV1"` | 128 detectors | a `ZodError` from `createScannerManifestV1` whose issue path is `protocol` | none |
| Baseline vet request v1 | `src/baseline/batch-v1.ts:76` | `protocol: "BaselineVetRequestV1"`, `profile: "aih-baseline-v1"` | 100 components, 4096 paths each | `TypeError("invalid BaselineVetRequestV1: …")` | Core imports the producer through the published export map only |
| Baseline vet receipt v1 | `src/baseline/batch-v1.ts:113` | `protocol: "BaselineVetReceiptV1"` | 16 MiB per annex (`src/baseline/batch-v1.ts:40`) | `TypeError("invalid BaselineVetReceiptV1: …")` | as above |
| Organization evidence envelope v1 (producer side) | `src/core/organization-evidence-envelope-v1.ts:16` | `format: "aih-organization-evidence"`, `version: 1` (`src/core/organization-evidence-envelope-v1.ts:17`) | no byte bound of its own: it is projected only from an already verified attestation, whose payload is bounded as above | `TypeError` from the producer | Core owns the schema (`ai-harness/schemas/aih-organization-evidence-envelope-v1.schema.json`); Scan locks its bytes by digest, see below |

## Contracts Scan reads

| Contract | Authoritative definition | Identity | Refusal of an unknown identity |
| --- | --- | --- | --- |
| Core contract lock v2 — accepted pairs | `src/core/core-contract-lock-v2.ts:19` | `AI_HARNESS_CORE_CONTRACTS_ACCEPTED`, each entry a Core commit with the exact decision-schema digest Core carries at that commit, newest last; the two sets below are derived from it | `"schema digest mismatch"` for an accepted commit presented with another accepted commit's digest or schema bytes |
| Core contract lock v2 — accepted commits | `src/core/core-contract-lock-v2.ts:35` | `AI_HARNESS_STRICT_V2_COMMIT_ACCEPTED`, newest last; the newest is the default emitted `AI_HARNESS_STRICT_V2_COMMIT` | `"unexpected Core commit"` for a commit outside the set |
| Core contract lock v2 — accepted decision-schema digests | `src/core/core-contract-lock-v2.ts:39` | `AI_HARNESS_DECISION_V2_SCHEMA_SHA256_ACCEPTED`, newest last; the newest is the default emitted `AI_HARNESS_DECISION_V2_SCHEMA_SHA256` | `"schema digest mismatch"` for a digest outside the set, and for bytes that do not hash to the declared digest |
| Core organization-evidence envelope schema | `src/core/core-contract-lock-v2.ts:49` | one pinned digest, `88c0a36e9177201660e773351958d89059c7d5b54e1c437d0afd06f48c5288bc`; the schema is byte-identical at every accepted Core commit | `"unexpected organization evidence schema digest"` |
| Core checkout gate | `tools/verify-core-contract-lock-v2.mjs` | a Core tree at an accepted commit, paired with that commit's exact decision-schema digest | `"unexpected Core commit"`, `"schema digest drift: <path>"`, `"a tree without git history requires --core-commit"`, `"declared commit is not the checked-out commit"` |

**Why a set and not one value.** Core's governance-decision schema changed additively
between the two accepted commits. A single pinned digest made the gate pass forever
against one old Core while newer Core artifacts went unrecognised; re-pinning to the new
digest alone would have silently stopped accepting evidence that is still valid.
Accepting a digest states only that this Scanner knows that contract and can read
evidence produced against it. It is not approval of a Core release, and it is not a
range: a third value is refused by name.

## Records Scan hands to a consumer

| Contract | Authoritative definition | Identity | Refusal of an unknown identity |
| --- | --- | --- | --- |
| Scan result record v1 | `src/scan-result-record.ts:30` | `format: "aih-scan-result-record"`, `version: 1` (`src/scan-result-record.ts:31`), `subject.name: "source-tree"` (`src/scan-result-record.ts:32`) | `parseScanResultRecordV1` (`src/scan-result-record.ts:459`) returns `{status:"refused", reason}` with `unknown-format`, `unknown-version`, `unknown-subject-name`, `malformed-record` or `not-an-object`. It checks the declared identity only, so `{status:"read"}` carries only a new frozen `ScanResultRecordIdentityV1` (`format`, `version`, `subject`), never the rest of the record: a verified record comes only from `readScanResultRecordV1` |
| Detector capability v1 | `src/capability/detector-capability-v1.ts:172` | `protocol: "DetectorCapabilityV1"`, `contracts.capabilityVersion: 1`, `capabilitySha256` over the canonical record. Each execution profile summary carries its own `supportedPlatforms` and `prerequisites`, and the capability's restate its default profile's. `emptySource: "completes" \| "refused"` says what a run does with a source root that has no entries: `detector.semgrep`, `detector.skillspector`, `detector.snyk-agent-scan`, `detector.aih-trust-lint` and `detector.aih-binding-gate` complete. Every uv-backed profile summary publishes `analyzerLock: { path, sha256 }`, the bundled uv.lock it installs and that file's sha256 as shipped, read at module load (an unreadable lock fails the load as an incomplete install). Phase B2 registers `detector.aih-trust-lint` and `detector.aih-binding-gate` (in-process, every platform, `emptySource: "completes"`), `detector.cisco-mcp-scanner` (`host-process-uv-v1` only, Linux `amd64` and `arm64`, because litellm 1.93.0 is manylinux-only) and `detector.snyk-agent-scan` (`host-process-uv-v1` only, a required `environment-variable` prerequisite `SNYK_TOKEN` read from the request env), and adds `source-tree` to `detector.cisco`, accepted only under `host-process-uv-v1`. Prerequisite kinds add `host-executable` (a name resolved from the declared PATH, then fixed well-known directories, and probed that way) and `uv-python` (a Python version request uv satisfies without downloading, reported `not-probed`). Adding the host profiles, `emptySource` and the `.git` scope sentences changed every `capabilitySha256` | `resolveDetectorCapabilityV1` returns `undefined` for a detector this package does not own |
| Detector execution profile document v1 | `src/capability/detector-capability-v1.ts:136` | `protocol: "DetectorExecutionProfileDocumentV1"`; its canonical sha256 is the capability's `executionProfile.sha256`, and a document's digest changes only when the document does. `host-process-uv-v1` is offered by `detector.semgrep`, `detector.cisco`, `detector.cisco-mcp-scanner` and `detector.snyk-agent-scan` (the last two have no other profile), runs only when named by `executionProfileId` and is never a default or a fallback; it declares `isolation: "none"` and `network: "unenforced"` and runs Semgrep and Cisco on Linux `amd64` and `arm64`, macOS `arm64` and Windows `amd64`, the hosts where every locked analyzer dependency has an exact-pinned binary wheel (macOS `amd64` lacks one for cryptography 50.0.0 and Windows `arm64` for Semgrep, so both are refused `unsupported-platform`); `detector.cisco-mcp-scanner` runs on Linux `amd64` and `arm64` only (its lock pins litellm 1.93.0, which publishes manylinux wheels only), and `detector.snyk-agent-scan` on the platform set recorded for its pinned version (`snykAgentScanPlatformSupportV1`): at 0.6.4 Linux `amd64` and `arm64`, macOS `arm64` and Windows `amd64` (0.6.4 guards its POSIX-only `pwd` imports; macOS `amd64` and Windows `arm64` lack a cryptography 50.0.0 wheel), where 0.5.17 excluded Windows. It resolves uv from the declared PATH or a well-known directory, lets uv discover a CPython `3.12` with `--no-python-downloads`, fills a persistent Scan-owned uv cache addressed by the lock digest (the scan stage runs `--offline`), contains every spawn in a POSIX process group or a Windows Job Object that the analyzer is created inside (`src/cli/windows-job-supervisor.ts:560`; cancellation asks the supervisor to terminate the job and kills it only after a bounded grace) and kills and fails on any process still naming the run's private directories afterwards (`src/cli/residual-processes.ts:221`): on Windows by command line or image, on Linux and macOS also by inherited environment or working directory. On POSIX a descendant that deliberately leaves the session and also clears its environment and working directory is not detectable and may outlive the run; the profile does not contain a hostile analyzer, and `linux-namespace-uv-v1` is the containment option on Linux. Its environment policy is `fixed-values-by-os` (`src/baseline/runtime-v1.ts:224`): each spawn's whole environment per OS, plus the only caller variables Python discovery reads. Cisco installs the same `cisco-skill-scanner` lock under this profile and `linux-namespace-uv-v1` (skill-scanner 2.1.0, litellm 1.102.1, no win-unicode-console), whose wheels exist on those hosts. `docker-host-local-skillspector-v1` never pulls: through the Docker CLI found on PATH and the host's current context, with a private DOCKER_CONFIG, it inspects only the local tag `skillspector:aih-c7958a3268d9` (SkillSpector v2.12.0, revision c7958a32, controlled digest `sha256:efe47bd7…`, published at `ghcr.io/samartomar/skillspector` by that digest) (`src/baseline/runtime-v1.ts:58`), admits that image when its `Id` is the pinned digest or a caller-accepted one (run by that bare digest) or else when a `RepoDigests` entry is, whole or by its `@` suffix (run by that full entry), runs it with `--pull never` and `--network none`, and removes the container by name when the client is ended on a timeout, an abort or a failure; an absent tag or an image carrying no allowed digest fails at stage `availability` naming the pinned digest (Docker had to be asked, so it is not a refusal). The pulling `docker-host-skillspector-v1` is removed. `in-process-trust-lint-v1` and `in-process-binding-gate-v1` (exported as `IN_PROCESS_TRUST_LINT_PROFILE_V1` and `IN_PROCESS_BINDING_GATE_PROFILE_V1`) are in-process profiles for every platform that spawn nothing and need no prerequisite; the detectors that use them register them. `oci-hardened-cisco-v1` lists `/usr/bin/docker`, the one executable its broker spawns. Every SARIF-producing document states that artifact URIs are made source-relative before the annex digest is taken | `resolveDetectorExecutionProfileDocumentV1` returns `undefined` for an unknown profile id |
| Detector run result v1 | `src/runner/run-detector-v1.ts:803` | the `outcome` discriminant; refusal reasons at `src/runner/run-detector-v1.ts:153` | `runDetectorV1` returns `{outcome:"refused", reason, detail}`; `unknown-detector`, `unsupported-platform`, `unsupported-subject-kind`, `subject-requirement-unmet`, `prerequisite-missing`, `execution-profile-unavailable`, `detector-options-invalid`. Refusals and failures are returned values, never thrown, and every refusal happens before any process is spawned. A detector whose only profile is `host-process-uv-v1` (`detector.cisco-mcp-scanner`, `detector.snyk-agent-scan`) is refused `execution-profile-unavailable` unless `executionProfileId` names it. `detector.snyk-agent-scan` reads `env` as its request env, exactly `{SNYK_TOKEN}` (absent or blank is `prerequisite-missing` "SNYK_TOKEN is not set", any other key `detector-options-invalid`), resolves uv from the process environment, and passes the token only to the scan spawn; its failure details are fixed text with exit status and byte counts. Its report completes, zero findings included, only when every snyk-agent-scan 0.5.17 scan-path entry proves analysis of the subject (C2a §5.3 exact predicates, `src/detectors/snyk-agent-scan/index.ts`): the entry has no ScanError, `servers` is an array and every server carries a ServerSignature and no ScanError, and the entry is bound by existing realpaths: an empty discovery (`servers: []`) only under the tree itself as key, a rule applied to every entry so an unrelated empty entry beside the root's entry fails, otherwise a key in the tree with every server's subject path (a SkillServer's `server.path`, else its `config_path`) in the tree, or the tree's parent as key with every server a SkillServer at the tree itself; one unbound entry or server fails the whole report at `output`. Nested records are validated partially: only the 0.5.17 model fields C2a §5.3 lists are checked (optional fields may be omitted; entity contents inside a ServerSignature are not validated). A server whose `error` is an object with `is_failure === false` is never analyzed, even when 0.5.17's `verify_api.py` recovered its signature server-side: an intentional limitation (S2g) while no real recovery output has been captured. The in-process engines (`detector.aih-trust-lint`, `detector.aih-binding-gate`) read the declared source root without a snapshot, sealed before and after, and check the signal and the time budget before starting and before keeping the result. The uv engines run through `runHostUvEngineV1` (`src/baseline/runtime-v1.ts`), which accepts only the engine's exact locked offline uv argv and gives it host-process-uv-v1's containment, residual sweep and private-directory removal; a spawn Scan ended is reported with its cause after the engine returns. Platform and prerequisite gates are those of the selected execution profile, and a missing prerequisite is refused, never answered by switching profile. The promise never rejects: the request is read once, so an accessor that throws on the request refuses `unknown-detector` (the class of a request that is not an object) and one on its subject refuses `subject-requirement-unmet`, each detail naming the field; a request that cannot be inspected at all, such as a revoked Proxy, also refuses `unknown-detector`; an `executionProfileId` that is present but not a non-empty string, an `env` that is not an object of string (or absent) values, a `signal` that is not an `AbortSignal` (`src/runner/run-detector-v1.ts:251`), a `timeoutMs` outside 100 to 3,600,000 whole milliseconds (`src/runner/run-detector-v1.ts:256`) and an `ociCapture` that is not an object are refused `execution-profile-unavailable` before any of them is interpolated or used, the detail naming the field and its shape but never coercing its value; the OCI capture profile refuses a `signal` or `timeoutMs` it cannot yet honour. Aborting the signal or exhausting the budget ends the analyzer's whole process tree and returns `failed` with `failure.cause` (`src/runner/run-detector-v1.ts:176`) `cancelled` or `timed-out`; processes that outlived the analyzer are killed and the run fails with cause `residual-processes`, and a tree that cannot be proven gone with `containment-failure`; an already aborted signal fails at stage `availability` before anything starts. Every observation run seals the tree before and after with `SourceObservationSealV1` (`sourceSeal`, `src/runner/run-detector-v1.ts:347`); the OCI capture profile keeps `SourceSealV2`. A source root with no entries completes only for a detector whose capability says `emptySource: "completes"` (Semgrep, SkillSpector, snyk-agent-scan, aih-trust-lint, aih-binding-gate), with the empty tree's seal and coverage; otherwise it is refused `subject-requirement-unmet`. An empty selection over a non-empty tree is accepted. Every `succeeded` and `failed` result's `executionProfile.id` is the requested `executionProfileId` when one is named (the default profile's otherwise), for every observation profile (`tests/runner/run-detector-v1-profile-echo.test.ts`). A `host-process-uv-v1` observation records `hostRuntime` (`src/runner/run-detector-v1.ts:216`): the uv path, version and where it was found, the Python request, path and version, the uv cache key and the containment used. A run that executed but whose private analyzer snapshot cannot then be removed is `failed` at stage `cleanup` if it had otherwise succeeded, and keeps its own earlier failure if it had not; a caller-supplied `prerequisiteProbe` that throws, is not callable or answers outside `present`, `missing` and `not-probed` makes the run `failed` at stage `availability`, with that prerequisite and every later one reported `not-probed`. A `skill-directory` selection that holds nested `SKILL.md` files and none at its top is refused `subject-requirement-unmet` with a detail saying Scan runs one skill root per request and how to shard. (S2h) Every JSON or SARIF text an analyzer emits is parsed by one strict RFC 8259 parser (`src/contract/strict-json-v1.ts`): malformed UTF-8 (stdout is decoded fatally), a byte-order mark, trailing data, non-JSON whitespace, a repeated object key at any depth, a number that cannot be held exactly or nesting beyond 512 fails the run at `output`, and nothing is repaired. (S2i) Whether a number can be held is decided on the text's exact decimal value, never its spelling: an integer value must be exactly the double (so `9007199254740992` is accepted and `9007199254740993`, `9007199254740993e0` and `9007199254740993.0` are refused alike), any other value must be exactly the double or its shortest round-trip decimal, and overflow, underflow of a non-zero value and negative zero are refused; each refusal is a typed `StrictJsonNumberErrorV1` (`src/contract/strict-json-v1.ts`). (U1g) One number token is at most 1100 characters (`STRICT_JSON_MAX_NUMBER_CHARACTERS_V1`, above the 1077 of the longest exact double spelling), checked while it is scanned and before any work on its value; a longer token is a typed `StrictJsonBoundErrorV1` (`bound: "number-characters"`), and number text is read by linear scans, never a regular expression. (U1g) A Cisco JSON report the strict parser refuses, or whose shape is malformed, fails at `output` (it failed at `execution` before), naming the parser's reason; a skipped skill or a skill-count mismatch stays a `coverage` failure. On Linux and macOS a source entry whose name holds a backslash, a control character or bytes that are not UTF-8 is refused `subject-requirement-unmet` before anything is sealed or run, the detail naming the path JSON-escaped; a backslash is never read as a separator, and Windows is unchanged. A run that started (`failed` or `succeeded`) also names the package that executed it, `producer: {name:"@aihq/scan", version}` (`src/runner/run-detector-v1.ts:298`), read from the manifest shipped beside that code and `null` rather than guessed when unreadable; a refusal executed nothing and carries none |
| Detector availability probe | `src/runner/run-detector-v1.ts:1394`, result `src/runner/run-detector-v1.ts:1353` | `probeDetectorAvailabilityV1({detectorId, executionProfileId, env?, signal?, acceptedImageDigests?, prerequisiteProbe?})` returns `{available: true, detectorId, analyzerVersion, executionProfile, prerequisites, seams}` or `{available: false, reason, detail}` | takes no subject and spawns, pulls and executes nothing. It applies runDetectorV1's request-field, profile, `acceptedImageDigests`, platform and prerequisite gates and answers with the same refusal reasons; an absent `executionProfileId` is refused `execution-profile-unavailable` (a probe always names its profile); an already aborted signal or a misbehaving `prerequisiteProbe` answers `availability-failed`. As for a run, `detector.snyk-agent-scan`'s `env` is the request env of `SNYK_TOKEN` alone (absent or blank answers `prerequisite-missing` naming the variable, any other key `detector-options-invalid`) and its uv is found on the host environment. `available: true` means no required prerequisite is known to be missing: a container image, a uv-discoverable Python and a reachable index stay `not-probed` and are settled only by a run. The promise never rejects |
| Cisco shard run | `src/runner/run-cisco-shard-v1.ts:142`, result `src/runner/run-cisco-shard-v1.ts:52` | `runCiscoShardV1({sourceRoot, jobs: [{id, path, inputSha256}], expected: {analyzerVersion, lockSha256}, executionProfileId, concurrency, env?, signal?, timeoutMs?, runner?})` returns `{outcome: "succeeded", executionProfile, producer, analyzer: {version, lockSha256}, outputs: [{jobId, path, inputSha256, sarif, sha256}], sourceSeal: {before, after}}`, `{outcome: "refused", reason, detail}` or `{outcome: "failed", failure: {stage, detail, cause?}, executionProfile, producer}` (S2g) Before a shard returns, every result of every job's normalized SARIF must name only files of that job's own sealed inventory: at least one location, each `locations` URI an exact, case-sensitive root-relative path of a file sealed under the job path immediately before its scan, and each related location that names a URI likewise; otherwise the shard fails at `output`, lowest index first, with no partial outputs. (S2h) Every other artifact location of a result (related, code-flow and thread-flow locations, `analysisTarget`; not `properties`) and the run's `threadFlowLocations` and `graphs` are bound to the same sealed inventory; an `artifactLocation.index` resolves through `run.artifacts[index].location.uri`, and a malformed or unresolved index, an index whose file is not sealed for the job, or a `uri` and `index` that name different files fails at `output`. (S2i) A result's `analysisTarget` is normalized like every other location (base resolved, job prefix applied) before it is bound, so a job-relative `{uri:"SKILL.md"}` binds to the job's `SKILL.md` and the root-relative spelling `skills/alpha/SKILL.md` names `skills/alpha/skills/alpha/SKILL.md` and fails. (U1f, owner decision D1) On Windows only, each job's SARIF is first bound over that job's own sealed files by the rule of the Scan findings row: a result, related or run artifact location that is not exactly one of them takes the unique one equal under `String.prototype.toLowerCase`, and the job SARIF carries that real name before completion evidence is attached and before its `sha256` is computed; several matches fail the job at `output` (lowest index first), no match still fails as unbound, and Linux and macOS stay exact. | C2a §3.7. Only `host-process-uv-v1` runs a shard in this build; `linux-namespace-uv-v1`, any other profile and an absent `executionProfileId` are refused `execution-profile-unavailable`. `expected.lockSha256` must equal that profile's published `analyzerLock.sha256` or the request is refused `analyzer-lock-mismatch`; a malformed or unreadable request, a job path that is not a safe POSIX source-relative path to a contained directory holding a `SKILL.md`, a duplicate job id, a concurrency outside 1..64 or (S2h) a job path holding an entry name the source-relative form cannot carry (a POSIX backslash, control character or non-UTF-8 bytes; the detail names the job and the path) is refused `shard-request-invalid`; all before any process is spawned. The expected analyzer version before any `+` suffix is checked by the version gate (`availability`). Job paths are sealed at the start and the end and each job's input identity is re-proven around its scan (`coverage`, "source changed"). Outputs come back in job order, each SARIF's URIs prefixed with its job path and carried with its sha256; on any failure no new job starts, the lowest-index failure is reported and no output is returned. The promise never rejects |
| Scan completion evidence v1 | C2a §1.6 "Completion evidence v1 [Scan: S2g]"; written by `src/detectors/completion-evidence-v1.ts:18`, digest `src/detectors/completion-evidence-v1.ts:57`, attached by `src/detectors/completion-evidence-v1.ts:183` | every run of the SARIF of a succeeded `runDetectorV1` result (the eight SARIF producers: `detector.aih-trust-lint`, `detector.aih-binding-gate`, `detector.cisco` for a skill-directory and for a source-tree subject, `detector.cisco-mcp-scanner`, `detector.snyk-agent-scan`, `detector.skillspector`, `detector.semgrep`) and of every `runCiscoShardV1` job output carries `invocations[0].executionSuccessful: true` and `invocations[0].properties.aihScanCompletionV1 = {detectorId, subjectTreeSha256, analyzedFileCount, analyzer: {version, lockSha256}}`, exactly those keys and equal in every run of one log. Scan writes it only after the analyzer's own completion proof, the location rules, the equal re-seal and (for a shard job) the result binding passed; analyzer SARIF keeps its invocations and gains the key in `invocations[0].properties`, Scan-built SARIF (trust lint, binding gate, mcp-scanner, Snyk) gains `invocations: [{executionSuccessful: true, properties}]`, and (S2h, D16) every Scan-built run carries the SARIF 2.1.0 `tool.driver {name, version}` (`aih-trust-lint` 1.0.0, `aih-binding-gate` 1.0.0, `mcp-scanner` and `snyk-agent-scan` at the pinned analyzer version). `subjectTreeSha256` is subject-files-v1: sha256 over, for the subject files sorted by root-relative POSIX path in UTF-16 code-unit order, `UTF-8(path) 0x00 lowercase-hex-sha256 0x0A` (a file link keyed by its link path over its target's bytes); `analyzedFileCount` is the number of those files. The subject is every sealed file for Semgrep and SkillSpector (top-level `.git` included); every sealed file outside top-level `.git` for Cisco skill-directory and Snyk; those files inside a job directory for Cisco source-tree; the sealed `mcpConfigPaths` for mcp-scanner; the selection for trust lint and binding gate; the job's component inventory for a shard job. `analyzer.version` is the observation's `analyzerVersion` (the shard's `analyzer.version`); `analyzer.lockSha256` is the profile's `analyzerLock.sha256`, or null for the in-process and SkillSpector Docker profiles. A count of 0 is allowed only for the `emptySource: "completes"` detectors (Semgrep, SkillSpector, Snyk, trust lint, binding gate). `detector.aih-native` and the OCI capture profile carry no SARIF and are out of scope | Scan fails the run at `output` instead of writing it: a log with no runs, a run without a non-empty `invocations` array, a first invocation that is not `executionSuccessful: true` or whose `properties` is present and is not an object (S2h: `null`, `false`, `0` and strings included; only an absent `properties` is created), an incoming `aihScanCompletionV1` in any invocation (a forgery, never overwritten), or a zero count the empty-source rule does not allow. Core fails closed on a succeeded SARIF result without it |
| Detector options (request field) | `src/runner/run-detector-v1.ts:249`, checked by `src/runner/detector-options-v1.ts:68` | `detectorOptions`, read once into a frozen copy: `detector.cisco` takes exactly `{concurrency}` (a whole number 1 to 64, default 4), applied only to a `source-tree` subject under `host-process-uv-v1`, which runs one skill-scanner job per directory holding a selected `SKILL.md` with at most that many at once, merged in job order, the lowest-index failing job reported and no partial SARIF kept (`src/runner/engine-dispatch-v1.ts`, engine `runCiscoSourceTreeScanV1`, host lock `cisco-skill-scanner-host`); any other Cisco request runs one scan, so a valid concurrency is refused there with a detail saying where it applies; `detector.aih-trust-lint` requires exactly `{internalScopes, mcpConfigPaths}`; `detector.cisco-mcp-scanner` requires exactly `{mcpConfigPaths}`; `detector.aih-binding-gate` takes none or an empty object; every other detector takes none. The scope and path rules are the engines' own validators (`trustLintInternalScopesProblemV1`, `mcpConfigPathsProblemV1`), so the runner and the engines refuse exactly the same requests | any violation is refused `detector-options-invalid` before the source is read, never coerced or repaired: an unknown or missing key, a non-object, an accessor that throws, `internalScopes` that are not 0 to 256 normalized npm scopes (`^@[a-z0-9][a-z0-9._~-]*$`), unique and in `localeCompare` order, or `mcpConfigPaths` that are not 0 to 1,024 safe relative POSIX paths, each one of Core's incoming MCP config names (`.mcp.json`, `.cursor/mcp.json`, `.kiro/settings/mcp.json`, `mcp-configs/mcp-servers.json`, `.vscode/mcp.json`, `opencode.json`, `mcp.json`) under the root or under a directory whose `SKILL.md` is selected, unique and in Core's discovery order (the root, then each selected skill directory in Core's `localeCompare` order, not the selection order, names in that order). After sealing, a config path absent from the sealed tree is refused the same way; one that is a directory or a symlink is accepted |
| Accepted SkillSpector image digests (request field) | `src/runner/run-detector-v1.ts:274`, checked by `src/baseline/runtime-v1.ts:744` | `acceptedImageDigests`: 1 to 32 distinct `sha256:` + 64 lowercase hex digests, for `docker-hardened-skillspector-v1` and `docker-host-local-skillspector-v1` only (the local profile admits its local tag's image by them as described in the profile document row; the rest of this row describes the hardened profile); the image that ran is recorded as `observation.image` (`src/runner/run-detector-v1.ts:214`), `{digest, reference, acceptance: "scan-pinned" \| "caller-accepted", pinnedPullFailure?}`, and `analyzerVersion` names the matched digest | a malformed list, or one given to another profile, is refused `execution-profile-unavailable` before anything is spawned. The list never replaces Scan's own acquisition. Scan's pinned image, when present, runs as `scan-pinned`; when absent, Scan attempts its pinned pull exactly as without the list, and an image that pull yields also runs as `scan-pinned`. The pinned digest check is unchanged: a pinned reference that resolves to another image still fails, whatever the list says. Only if the pinned image is still absent after that pull (refused, failed, offline or not runnable) are the accepted digests inspected, in order, against local images only; the first present one runs by its local image ID as `caller-accepted`, with the pinned pull's bounded failure reason in `pinnedPullFailure`, and nothing more is pulled. When none is present the run is `failed` at stage `availability` rather than refused, because Docker had to be asked; without the list a failed pinned pull stays a `failed` result at stage `acquisition`. `caller-accepted` means Scan verified the digest the caller named, not that image's provenance or source revision; the profile document still names Scan's pinned image, so a reader takes the executed image from `observation.image` |
| Scan coverage v1 | `src/runner/run-detector-v1.ts:178` | `kind`, plus `coveredPaths`, `excludedPaths` and `uncoveredPaths`, so `complete` is computed rather than claimed. Semgrep and SkillSpector are given the whole tree, a top-level `.git` and dependency or build directories included, as Core's own runs are; the native and Cisco analyzers are not given a top-level `.git`, so its files, or (S2h) a worktree's `.git` file itself, are `uncoveredPaths`, never `coveredPaths` | n/a — it is emitted, never parsed |
| Scan findings v1 | `src/findings/scan-findings-v1.ts:66` | `protocol: "ScanFindingsV1"`, `source: "annex" \| "attestation-facts-only" \| "analyzer-output-digest-bound" \| "analyzer-sarif"`. A SARIF observation from `runDetectorV1` is projected by `projectAnalyzerSarifFindingsV1` (`src/findings/scan-findings-v1.ts:414`) as `analyzer-sarif`: from the digest-verified annex bytes only, each result must name a rule and a first location that is a sealed source file, and its fingerprint is the `raw-occurrence-v1` formula with the analyzer as detector class. For the B2 engine detectors (`aih-trust-lint`, `aih-binding-gate`, `cisco-mcp-scanner`, `snyk-agent-scan`) the runner passes `unboundLocations: "unavailable"` and `maxResults: 100000`: a result whose first location names no sealed file (a whole-tree finding, `untrusted-document`, `.`) keeps its rule, level and message, reports its location unavailable with the gap kind `finding-location-not-a-sealed-file`, and is fingerprinted over the URI as written with an all-zero file digest; the native identity observation stays `analyzer-output-digest-bound`. `detector.cisco` source-tree jobs are dispatched as an engine but stay bound: a job result naming a file that is not sealed fails at `output`, as the skill-directory run does. Cisco 2.1.0 reports `os.path.normcase` paths, which Windows lowercases (`skills/injected/skill.md` for `skills/injected/SKILL.md`); on win32 only, every `detector.cisco` run (skill directory and source tree, and each `runCiscoShardV1` job over its own files) binds a result, related or artifact location that is not exactly a sealed file to the unique sealed file whose whole source-relative path is equal under `String.prototype.toLowerCase` on both sides, and the SARIF and the finding carry the sealed file's real name (`src/baseline/cisco-sealed-case-binding-v1.ts`). That comparison is the Unicode default lowercase mapping, not case folding and not a normalization; no match still fails at `output`, and more than one match fails at `output`. Linux and macOS stay exact | `readScanFindingsV1` (`src/findings/scan-findings-v1.ts:576`) returns `{status:"unverified"}` for anything not minted by the verifier, and `{status:"invalid-input", reason}` when annex bytes do not match the declared descriptor or an entry does not bind to exactly one declared occurrence fact; the projection throws, and the run fails at stage `output`, on any SARIF result it cannot bind, on a log with no runs and on a run without a results list. Before any projection, every analyzer SARIF (Semgrep, SkillSpector, Cisco skill-directory, and each Cisco source-tree or shard job) must prove a completed analysis (`src/detectors/sarif-completion-v1.ts`): version 2.1.0, runs with a tool driver, a results list and invocations; a shortfall fails at `output`, and so does (S2f; exact predicates S2g) a result that is not an object, a result `locations` that is present and is not an array of location objects (a location is an object; its `physicalLocation`, when present, an object; that one's `artifactLocation`, when present, an object; its `uri`, when present, a string), a notification list (`toolExecutionNotifications`, `toolConfigurationNotifications`) that is present and is not an array, a notification that is not an object, a notification `level` that is present and is not none/note/warning/error, or a notification `message` that is present and is not a message object (`text` or `id` present, each a string when present, `markdown` a string when present, `arguments` an array of strings when present). Nested validation is partial and stops there: a notification's other properties (`locations`, `descriptor`, `exception`, …) and a result's other fields are not checked by this step (URIs are the location rules' job, and the findings projection reads what it uses); an invocation that is not an object with `executionSuccessful: true`, or that carries a well-formed error-level notification, fails at `execution` |
| Source-relative SARIF artifact URIs | `src/baseline/sarif-source-relative-v1.ts:597`, and for Cisco `src/baseline/sarif-source-relative-v1.ts:812` | every `artifactLocation.uri` (and run artifact location) of an observation's SARIF is relative to the declared source root, with forward slashes: the `/aih/source` and `/scan` mounts and the private snapshot root (either Windows spelling) are removed. Each `uriBaseId` is first resolved through its run's `originalUriBaseIds` (chained bases followed; an undeclared `%SRCROOT%` is the root the analyzer was given), and a base outside the source root, an undeclared other base or a base cycle fails closed; the maps are then dropped, and a `uriBaseId` other than `%SRCROOT%` is removed with them. Cisco `scan-all` results, which are relative to each skill's own directory, are paired in order with its JSON report and prefixed with that skill's directory; every Cisco location's base is resolved and validated first (an undeclared `%SRCROOT%` is that skill's directory), and the first location must resolve to the file its JSON finding names. (U1f) Every other location inside a result (further, related and code-flow locations) is resolved against that result's paired skill with the same checks, before any sealed-file binding; a location outside every result (a run artifact or a notification) whose URI is relative to the analyzer's `%SRCROOT%` names no skill and fails at `output`, while an absolute URI or one under a base that resolves inside the source root is kept. A relative reference is joined to a `file:` base in URI space and percent-decoded once before containment is checked. (S2g) Only a location under a run invocation's `toolExecutionNotifications` or `toolConfigurationNotifications`, or a run artifact's own `location`, may name a directory: a URI ending in exactly one `/` whose path before that slash passes every rule above, rewritten with the slash kept (the pinned SkillSpector image reports a skipped `node_modules/`, also as `/scan/node_modules/`); the source root itself is never a directory URI, and result, related and code-flow locations must still name files. (S2i) A result's `analysisTarget` is an artifact location: its base is resolved and its URI rewritten under every rule above (for Cisco `scan-all`, relative to the reporting skill's directory). (U1g) `analysisTarget` is recognized only at `result.analysisTarget`, and nothing inside a `properties` bag is ever normalized, rewritten or refused (a bag's `analysisTarget` or `artifactLocation` is the analyzer's data); an `artifactLocation` or result `analysisTarget` that is not an object fails at `output`. The Cisco shard binding enumerates locations by the same rule. (U1g) Every `artifactLocation.index` resolves by one rule on every path (host runs, Cisco `scan-all`, source-tree and shard jobs), after its run is normalized: it must be a non-negative integer naming a run artifact whose location has a URI and names no other index, and a `uri` beside it must name the same file; a malformed, missing or out-of-range index, an index outside a run, a run artifact naming another index, or a disagreeing `uri` fails at `output` (`sarifArtifactLocationTargetV1`). (U1g) Cisco `scan-all`: every location a result carries (related, code-flow, stack, fix, analysis target; by URI or index), once resolved, must lie inside the directory of the skill that reported it (a based URI or an index naming a sibling skill fails at `output`; a nested skill's file stays acceptable for its parent). Every Cisco run of `runDetectorV1` (skill-directory and source-tree) then passes the shard's binding (`unboundCiscoSarifResultV1`): every one of those locations must name a file of the analyzed subject (the completion-evidence file set), after the win32 D1 binder, which now rebinds every artifact location by the same enumeration. (U1h) A result also names every location of the run-level objects it references, resolved for that result (`sarifResultSharedArtifactLocationsV1`): `run.threadFlowLocations[i]` through a thread-flow location's `index`, and `run.graphs[i]` through a graph traversal's `runGraphIndex` (a traversal names exactly one of `runGraphIndex` and `resultGraphIndex`, and a `resultGraphIndex` must name one of the result's own graphs). A malformed, out-of-range, unresolved or ambiguous reference, or a shared thread-flow location naming another index, fails at `output`. Those files obey the same rules as the result's own: Cisco `scan-all` keeps them in the reporting skill; every source-tree and shard job keeps every file its results name, directly or by a shared reference, in the job's skill directory (`ciscoJobSarifV1`: a sibling job's file fails at `output`, "not in the job's skill"); and `unboundCiscoSarifResultV1` binds them to the sealed files. (U1i) An artifact a location names by `index` lies inside its `parentIndex` ancestry, and the location reaches every ancestor (`sarifArtifactLocationFilesV1`): each ancestor must lie in the reporting skill (scan-all) or the job's skill directory (source-tree and shard jobs) and be a sealed file of the job or subject (`unboundCiscoSarifResultV1`). Every run artifact's `parentIndex` chain must resolve: a malformed or out-of-range parent, one naming another index, or a cycle fails at `output`. (U1h, coordinator decision D28) A Cisco JSON finding with `"file_path": null` (Cisco 2.1.0's skill-level findings, such as `LOW_ANALYZABILITY`) is accepted only as a skill-level finding at the reporting skill's own SKILL.md, through a bijective pairing. The finding must carry a string `id` and no line. Its SARIF counterpart (the result paired with it by position) must carry the same identity, JSON `(rule_id, id)` = SARIF `(ruleId, fingerprints.primaryLocationLineHash)`. That identity must occur exactly once among all JSON findings and exactly once among all SARIF results; any duplicate, across skills too, fails (U1i: an analyzer-supplied base cannot disambiguate it). The counterpart's first location must resolve to exactly `<skill>/SKILL.md`, and every location, index, D1 and sealed-file rule still applies. No counterpart, several counterparts, or a counterpart anywhere else fails at `output`, and no location is ever generated. The pairing proves no analysis: completion is decided by the coverage report, the invocation and (U1i, D30) the failed-analyzer rule, never by the pairing. The annex digest is taken over the rewritten canonical bytes | a URI outside the source root, one that escapes it, names the root itself or cannot be decoded, and any disagreement between Cisco's SARIF and JSON reports fail the run at stage `output`; nothing is guessed. Scan deliberately does not substitute Core's legacy fallback names (`semgrep.sarif`, `cisco.sarif`, `skillspector.sarif`, `mcp-scanner.json`, `.`) for an unmappable location (C2a §1.4): the coordinator decided the run fails closed instead, and no golden depends on the fallback |
| Cisco failed analyzers (coordinator decision D30, revised 20:58Z) | `src/baseline/cisco-analyzer-failures-v1.ts:93`, for `scan-all` `src/baseline/sarif-source-relative-v1.ts:967` | (U1i) A `detector.cisco` run (host `scan-all` under `host-process-uv-v1` and `linux-namespace-uv-v1`, every source-tree and shard job, the OCI capture) is complete only when every `analyzers_failed` entry of Cisco's JSON report is its documented fallback: analyzer `skill_loader`, the skill's only such entry, with a `SKILL_LOAD_FALLBACK_USED` finding in the same skill result and that finding's SARIF counterpart (for `scan-all` the result paired with it by position) located in the same skill. `scan-all` reports the list per `results[i]` (an entry at the report's top level names no skill and is never the fallback); the single-skill `scan` that jobs and the OCI capture now run with `--format sarif --format json --output-json <private file>` reports it at the top level. An absent key or an empty array is complete. The JSON report is read with the one strict parser and is never skipped. | Anything else fails at stage `coverage` with `Cisco reported failed analyzers: <analyzer> (<error>), … in <skill>[: <reason>]`, naming each analyzer and error. A malformed `analyzers_failed` (not an array; an entry that is not exactly a string `analyzer` and a string `error`), or a job's or the capture's JSON report that is missing, unreadable, not the single-skill shape or malformed, fails at stage `output`. |

**What an unavailable field means.** `FindingFieldV1`
(`src/findings/scan-findings-v1.ts:44`) is `present` or `unavailable` with a reason.
Findings are produced only from annex bytes whose digest matches the declared descriptor;
without those bytes every field except the occurrence fingerprint and its multiplicity is
`unavailable`. An empty findings list is never reported as "nothing was found": the gaps
say why the list is empty.

## Cisco OCI candidate size bound

`tools/verify-cisco-oci-candidate.mjs` refuses a candidate OCI layout with any file over
144 MiB or a layout over 192 MiB in total. These are measured values plus a stated margin:
the Cisco 2.1.0 candidate (linux/amd64, buildkit v0.30.0, gzip layers) has one venv layer of
144,783,569 bytes (138.1 MiB, because 2.1.0 ships compiled wheels) and a layout of
190,230,536 bytes (181.4 MiB), so the margins are 5.9 MiB and 10.6 MiB. The candidate keeps a
single copy of the environment: the final stage extracts the builder's tar through a bind
mount, so the tar itself is never a layer. A larger image changes these two numbers in a
reviewed commit; the bound is never removed.

## The reproducibility rule

Every compatibility run records the exact tarball sha256 of each package it tested, the
resolved version and dist-tag (or the git SHA for a branch-built tarball), and the sha256
of the consumer's `package-lock.json`. A promotion authorization is valid only for the
byte set it names; if `next` moves to different bytes, the earlier result cannot
authorize that candidate.

Scan's promotion gate (`.github/workflows/promotion-readiness.yml`) reads Core's
`core-sibling-compatibility` version 2 artifact and refuses version 1 by name. It accepts
only the one `scan-candidate` combination, which names Scan at `next` together with the
Core and Catalog it was installed with at `latest`, and it re-observes those two live:
if either `latest` moved or its bytes changed, the evidence no longer applies. Core owns
the artifact's shape; Scan only reads it.

These records are evidence for the gate, not dependency pins imposed on users. No
consumer is required to install a tested trio, and no release of one package requires a
release of the other two. Compatibility is decided by the declared `format` and `version`
of each artifact and by the accepted digest sets each package publishes — never by
version-number equality and never by a frozen trio. A recorded hash answers *what did we
test*; it never answers *what may you install*.

## Changing a contract here

A new accepted digest or an additive export is `semver:minor`
([VERSIONING.md](VERSIONING.md)). Removing an accepted value, renaming a format or
bumping a version is `semver:major`, because an installed consumer that reads the old
identity would start being refused. Add the new value to the accepted set, keep the old
one until the evidence produced against it is out of support, and move the default to the
newest member in the same change.
