# aih-scan self-hosting contract

Never run an installed aih-scan against this checkout. Until a CLI exists, this
is a manual bootstrap canon. After a CLI exists, exercise product behavior only
against temporary fixture roots, not this repository.

The manual maintenance keeps `AGENTS.md` and `CLAUDE.md` aligned with
`adapters/_shared-canonical-block.md`; direct TypeScript, test, lint, build, and
Git checks remain allowed here.
